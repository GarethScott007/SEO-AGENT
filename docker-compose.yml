version: '3.8'
services:
  seo-agent:
    build: .
    container_name: seo-agent
    env_file:
      - config/secrets.json
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
    cron: "0 3 * * * python /app/agent/daily_seo_agent.py"
